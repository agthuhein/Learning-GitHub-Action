apiVersion: v1
kind: Service
metadata:
  name: athapp-svc
  labels:
    app: athapp
spec:
  type: NodePort
  selector:
    app: athapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007
