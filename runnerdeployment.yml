apiVersion: actions.summerwind.dev/v1alpha1
kind: RunnerDeployment
metadata:
  name: my-runner-deployment # matches your current runner name
  namespace: arc-system
spec:
  replicas: 2
  template:
    spec:
      repository: agthuhein/Learning-GitHub-Action
      labels:
        - arc-demo
